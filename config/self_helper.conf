conky.config = {
    alignment = 'top_right',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    color1 = '#38bdf8', -- Sky Blue
    color2 = '#34d399', -- Emerald Green
    color3 = '#f87171', -- Coral Red
    font = 'DejaVu Sans Mono:size=10',
    gap_x = 30,
    gap_y = 60,
    minimum_width = 320,
    own_window = true,
    own_window_type = 'normal',
    own_window_class = 'Conky',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 160, -- Translucent background for high contrast
    own_window_colour = '#090d16', -- Dark premium slate background
    update_interval = 2.0,
    use_xft = true,
    double_buffer = true,
};

conky.text = [[
${color1}┌────────────────────────────────────────┐${color}
${color1}│        SELF-HELPER TELEMETRY           │${color}
${color1}└────────────────────────────────────────┘${color}
${execi 2 cat ~/.cache/self_helper/widget.txt}
${color1}──────────────────────────────────────────${color}
${color2}WEEKLY PERFORMANCE TRENDS:${color}
${execi 30 /home/vin/.local/bin/self_help_report.py --weekly | tail -n +5}
]];
